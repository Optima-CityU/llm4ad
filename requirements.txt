# Default dependencies for core usage, GUI, and common EoH examples such as TSP/OBP.
numpy<2.0.0
matplotlib
pytz
tqdm
ttkbootstrap

# Optional dependencies are installed only when needed.
# Examples:
#   pip install ".[all]"
#   pip install ".[machine-learning]"
#   pip install ".[science-discovery]"
#   pip install ".[partevo]"
#   pip install ".[llamea]"
